torch>=2.2,<3
pypdf>=5,<7
# For local notebook use only; Colab includes notebook tooling.
jupyter>=1.0
# Web chat interface (serve.py); the notebook and chat.py do not need these.
fastapi>=0.110,<1
uvicorn>=0.27,<1
